# Extern 变量映射工具使用说明

## 目录
1. [工具简介](#工具简介)
2. [快速开始](#快速开始)
3. [功能详解](#功能详解)
4. [操作指南](#操作指南)
5. [代码生成说明](#代码生成说明)
6. [常见问题](#常见问题)

---

## 工具简介

Extern 变量映射工具（`extern_mapper_gui.py` 是一个用于处理 C 语言 extern 变量与用户自定义变量之间映射和代码自动生成的图形化工具。该工具支持读取 C 头文件，解析结构体变量，构建变量映射关系，并自动生成赋值和转换代码，大幅提高开发效率。

### 主要特性

- ✅ 自动解析 C 头文件中的 extern 变量和结构体定义
- ✅ 直观的图形化界面，支持拖拽式操作
- ✅ 结构体用户变量简化操作
- ✅ 目标头文件变量支持
- ✅ 多条件与/或操作表达式构建
- ✅ Markdown 文档自动生成
- ✅ 自动代码生成，支持多种模板
- ✅ 配置导入/导出功能

### 版本信息

- **当前版本**: v2.1
- **开发语言**: Python 3
- **图形库**: Tkinter

---

## 快速开始

### 系统要求

- Python 3.8 或更高版本
- Tkinter 库（通常随 Python 一起安装）

### 安装与运行

1. 克隆或下载项目到本地
2. 运行工具：
```bash
python extern_mapper_gui.py
```

### 快速使用流程

1. **打开源文件** -> 选择包含 extern 声明的头文件
2. **选择变量** -> 在左侧列表选择要处理的 extern 变量
3. **添加用户变量** -> 在右侧配置目标变量
4. **配置映射** -> 建立 extern 成员与用户变量的映射
5. **生成代码** -> 点击生成按钮，获得目标代码
6. **导出文档** -> 生成 Markdown 说明文档

---

## 功能详解

### 1. 源文件解析

#### 支持的头文件

工具可以解析以下类型的 C 声明：

- extern 变量声明
- 结构体定义
- Typedef 类型声明
- 指针数组声明

#### 解析后的变量列表

在左侧面板显示解析后的 extern 变量列表，包括：
- 变量名称
- 变量类型
- 是否为结构体
- 成员详情（可展开查看）

### 2. 目标头文件支持

如果转换后的变量如果定义在另一个头文件中，可以：

1. 点击左侧面板的"浏览"按钮
2. 选择目标头文件
3. 在"目标头文件变量"标签页查看变量
4. 选择要导入的变量
5. 点击导入按钮

### 3. 用户变量管理

#### 单个添加

1. 在"用户变量"标签页中
2. 输入变量名、类型、描述
3. 点击"➕ 添加变量"

#### 从结构体添加

1. 点击"📦 从结构体添加"按钮
2. 选择已解析的结构体类型
3. 设置变量前缀
4. 选择是否展开成员
5. 点击添加

#### 批量添加

1. 点击"📋 批量添加"
2. 按格式输入变量列表
3. 点击添加

### 4. 映射关系配置

#### 基本映射

1. 选择 extern 成员（中间面板）
2. 选择用户变量（右侧面板）
3. 选择操作类型
4. 设置转换规则
5. 点击"➕ 添加映射"

#### 条件表达式构建

支持多条件与/或操作：

1. 在条件表达式区域
2. 添加多个条件（变量、操作符、值）
3. 设置条件间的逻辑连接（AND/OR）
4. 预览区实时显示完整表达式
5. 点击"➕ 添加条件"保存到映射

#### 自动匹配

点击"⚡ 自动匹配"可根据变量名相似性自动建立映射。

### 5. 代码与文档生成

#### 代码生成模板

- **赋值函数**: 生成完整的变量赋值函数
- **条件判断函数**: 生成条件检查函数
- **完整转换函数**: 同时包含检查和赋值
- **仅赋值语句**: 仅生成赋值操作
- **仅判断语句**: 仅生成条件判断

#### Markdown 文档

生成包含以下内容的详细文档：
- 源变量信息
- 目标变量信息
- 映射关系详细说明
- 代码示例模板
- 变量引用速查表

---

## 操作指南

### 详细步骤

#### 第一步：打开源文件

```
菜单：文件 -> 打开头文件
快捷键：Ctrl+O
```

选择包含 extern 声明的头文件，解析后变量会列在左侧面板。

#### 第二步：打开目标文件（可选）

```
菜单：文件 -> 打开目标头文件
快捷键：Ctrl+T
```

如果转换后的变量定义在另一个头文件中，可以打开它。

#### 第三步：添加用户变量

1. 点击"用户变量"标签页
2. 单个添加或批量添加
3. 或从目标文件导入变量
4. 或从结构体导入结构体成员

#### 第四步：配置映射关系

1. 在左侧选择 extern 变量
2. 在"映射关系"标签页配置

配置内容包括：
- extern 成员：源变量的成员
- 用户变量：目标变量
- 操作类型：
  - 直接赋值：无条件赋值
  - 条件赋值：满足条件才赋值
  - 逻辑判断：仅做条件判断
  - 自定义表达式：自定义处理
- 条件表达式（条件赋值时）
- 转换规则：
  - = ：直接
  - 强制转换：类型转换
  - 缩放：按比例
  - 偏移：加减常数
  - 自定义：自行公式

#### 第五步：生成代码

1. 点击"🔧 生成代码"
2. 在底部面板查看代码
3. 点击"📋 复制代码"到剪贴板
4. 或"💾 保存代码"到文件

#### 第六步：生成 Markdown 文档

1. 切换到"📝 Markdown 文档"标签页
2. 点击"📄 生成文档"
3. 查看或复制/保存文档

#### 第七步：保存/导入配置

```
菜单：文件 -> 导出映射配置
菜单：文件 -> 导入映射配置
```

---

## 代码生成说明

### 示例代码输出

#### 赋值函数示例

```c
/*
 * 自动生成的赋值函数
 * Extern 变量: g_sensor_data
 */

/* 目标头文件 extern 变量引用 */
extern target_sensor_t g_target_sensor;

void assign_from_g_sensor_data(
    sensor_data_t *g_sensor_data
) {
    if (g_sensor_data == NULL) return;

    g_target_sensor->temperature = g_sensor_data->temperature;
    
    if ((g_sensor_data->valid == true) && (g_sensor_data->humidity > 0)) {
        g_target_sensor->humidity = g_sensor_data->humidity;
    }
}
```

#### 完整转换函数示例

```c
int convert_from_g_sensor_data(...) {
    /* ── 条件检查 ── */
    if (!((g_sensor_data->valid == true) {
        return -1;
    }
    /* ── 赋值操作 ── */
    ...
    return 0;
}
```

### 变量引用规则

| 变量类型 | 引用方式 | 示例 |
|---------|---------|------|
| 源结构成员 | `<var>-><member>` | `g_sensor_data->temperature` |
| 目标结构成员 | `<var>-><member>` | `g_target_sensor->humidity` |
| 用户结构成员 | `(*<var>).<member>` | `(*my_sensor).temperature` |
| 简单用户变量 | `(*<var>)` | `(*my_temp)` |

---

## 常见问题

### Q1：找不到头文件？

**A**：请确认选择了正确的编码，包含 extern 声明。

### Q2：添加条件表达式如何处理多个与/或逻辑？

**A**：按顺序添加条件，每个条件选择与下一个条件的逻辑连接符（AND/OR），程序会自动处理括号和优先级。

### Q3：如何设置自定义转换公式？

**A**：选择"自定义"转换规则，在"自定义"输入框输入公式，例如：
- `* 1.2` 表示放大 1.2 倍，`+ 10` 表示加 10。

### Q4：自动匹配不成功？

**A**：基于变量名的最后部分的相似性，如 temperature 与源成员名匹配。

### Q5：导出的配置文件？

**A**：JSON 格式，可在下次使用导入配置。

---

## 更新日志

### v2.1 (最新
- ✨ 新增：Markdown 文档自动生成
- ✨ 新增：条件表达式与/或操作
- ✨ 新增：代码+文档组合复制功能
- 🔧 优化：代码生成优化
- 📚 文档：完善使用说明文档

### v2.0
- ✨ 新增：结构体用户变量简化操作
- ✨ 新增：目标头文件变量支持
- 🎨 界面：优化用户体验优化
