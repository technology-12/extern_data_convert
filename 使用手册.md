# Extern 变量映射工具 v4.0 使用手册

## 目录

1. [软件简介](#1-软件简介)
2. [安装与运行](#2-安装与运行)
3. [界面概览](#3-界面概览)
4. [基本操作](#4-基本操作)
5. [AI 大模型功能](#5-ai-大模型功能)
6. [映射文档工作流（v4.0 核心）](#6-映射文档工作流v40-核心)
7. [从文档导入映射关系](#7-从文档导入映射关系)
8. [快捷键](#8-快捷键)
9. [常见问题](#9-常见问题)

---

## 1. 软件简介

Extern 变量映射工具是一款用于 C 语言头文件中 extern 变量映射和代码生成的可视化工具。

### v4.0 核心工作流

```
自然语言文档 → 规范映射文档 → C 代码
```

**三阶段流水线设计：**

| 阶段 | 输入 | 输出 | 方式 |
|------|------|------|------|
| 阶段1 | 自然语言文档 + .h 文件 | 规范映射文档 | GUI导出 / AI转换 / 手动编写 |
| 阶段2 | 规范映射文档 | （可编辑的中间产物） | 人工审核/修改 |
| 阶段3 | 规范映射文档 + .h 文件 | C 代码 | AI生成 / 本地Python生成 |

### 主要功能

- **📋 映射文档工作流**：三阶段流水线，从自然语言到代码
- **🤖 AI 大模型集成**：连接 OpenAI 兼容 API，智能生成
- **🐍 本地代码生成**：无需 LLM，Python 解析映射文档生成代码
- **📥 文档导入映射**：从文档解析映射关系导入 GUI
- **📝 Markdown 文档生成**：生成清晰的变量映射关系文档
- **🔀 条件表达式**：支持与/或逻辑组合的条件判断

---

## 2. 安装与运行

### 基本运行（零额外依赖）

```bash
python extern_mapper_gui.py
```

或双击 `run_extern_mapper.bat`

### 可选依赖

```bash
# 读取 .docx 文档需要
pip install python-docx>=0.8.11
```

> **Win7 用户**：请使用 `python-docx==0.8.11` 版本

---

## 3. 界面概览

### 主界面布局

```
┌─────────────────────────────────────────────────┐
│  菜单栏: 文件 | 工具 | 🤖 AI 助手 | 帮助        │
├──────────┬──────────────┬────────────────────────┤
│ 源头文件  │  Extern 成员  │  用户变量 | 目标变量   │
│ 目标头文件 │  列表/详情    │  映射关系 | 条件表达式 │
├──────────┴──────────────┴────────────────────────┤
│ 底部标签页:                                       │
│ 💻 代码 | 📝 Markdown | 🤖 AI | 📋 映射文档工作流 │
├─────────────────────────────────────────────────┤
│ 状态栏                                            │
└─────────────────────────────────────────────────┘
```

### 底部标签页说明

| 标签页 | 功能 |
|--------|------|
| 💻 生成的代码 | 从 GUI 映射关系直接生成 C 代码 |
| 📝 Markdown 文档 | 生成 Markdown 格式的映射关系文档 |
| 🤖 AI 代码生成 | AI 基于当前 GUI 映射关系生成代码 |
| 📋 映射文档工作流 | **v4.0 核心** - 三阶段流水线 |

---

## 4. 基本操作

### 4.1 打开头文件

1. 菜单 → 文件 → 打开头文件（或 `Ctrl+O`）
2. 选择包含 extern 声明的 .h 文件
3. 工具自动解析 extern 变量和结构体定义

### 4.2 打开目标头文件

1. 菜单 → 文件 → 打开目标头文件（或 `Ctrl+T`）
2. 选择目标头文件
3. 在"目标头文件变量"标签页中查看变量

### 4.3 添加用户变量

- **单个添加**：输入变量名、类型，点击"添加"
- **批量添加**：点击"批量添加"，输入多行 `变量名:类型`
- **从结构体添加**：选择结构体类型，一键添加所有成员
- **从目标文件导入**：导入选中/全部目标变量

### 4.4 配置映射关系

1. 在"映射关系"标签页中操作
2. 选择 Extern 成员和用户变量
3. 选择操作类型（直接赋值/条件赋值/逻辑判断/自定义）
4. 可设置转换规则和条件表达式
5. **自动匹配**：按名称自动匹配成员

### 4.5 生成代码

- 点击"生成代码"按钮
- 选择代码模板（赋值函数/条件判断函数/完整转换函数等）
- 可复制、保存或编辑生成的代码

---

## 5. AI 大模型功能

### 5.1 配置 LLM 服务

1. 菜单 → 🤖 AI 助手 → ⚙️ LLM 服务设置
2. 填写配置信息：
   - **API Base URL**：如 `https://api.openai.com/v1`
   - **API Key**：你的 API 密钥
   - **模型名称**：如 `gpt-4o`、`deepseek-chat`
3. 或使用快速预设：

| 预设 | URL | 说明 |
|------|-----|------|
| OpenAI | https://api.openai.com/v1 | GPT-4o 等 |
| DeepSeek | https://api.deepseek.com/v1 | deepseek-chat |
| 通义千问 | https://dashscope.aliyuncs.com/compatible-mode/v1 | qwen-plus |
| 智谱GLM | https://open.bigmodel.cn/api/paas/v4 | glm-4 |
| Ollama | http://localhost:11434/v1 | 本地模型 |
| LM Studio | http://localhost:1234/v1 | 本地模型 |

4. 点击"测试连接"验证配置

### 5.2 AI 生成映射代码

在底部"🤖 AI 代码生成"标签页：
1. 先在 GUI 中配置好映射关系
2. 点击"🧠 AI 生成映射代码"
3. AI 基于当前映射关系和头文件生成 C 代码
4. 可复制/保存/导入到代码标签页

---

## 6. 映射文档工作流（v4.0 核心）

### 6.1 工作流概述

映射文档工作流是 v4.0 的核心功能，采用三阶段流水线设计：

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  阶段1: 输入  │ ──→ │ 阶段2: 映射文档 │ ──→ │ 阶段3: 代码  │
│ 自然语言文档  │     │ （可编辑）     │     │  C 语言代码  │
│ + 头文件     │     │              │     │             │
└─────────────┘     └──────────────┘     └─────────────┘
```

在底部 **"📋 映射文档工作流"** 标签页中操作。

### 6.2 阶段1：输入

**加载需求文档：**
- 点击"📄 加载文档"加载自然语言需求文档
- 支持 .txt、.md、.csv、.docx 格式

**选择关联头文件：**
- 从已打开的头文件中选择，或点击"📂 浏览..."选择

**生成映射文档的三种方式：**

| 方式 | 说明 | 需要 LLM |
|------|------|----------|
| 📤 从GUI映射导出 | 将当前 GUI 中配置的映射关系导出为映射文档 | 否 |
| 📂 加载映射文档 | 从文件加载已有的映射文档 | 否 |
| 🤖 AI: 自然语言→映射文档 | AI 分析自然语言文档生成映射文档 | 是 |

### 6.3 阶段2：规范映射文档

映射文档是工作流的中间产物，采用标准格式：

```markdown
# 数据映射关系文档

- 生成时间: 2026-01-01 12:00:00
- 源头文件: source.h
- 目标头文件: target.h

---

## 源变量定义

### g_sensor_data (struct SensorData *)

- temperature : float
- humidity : float
- pressure : uint32_t

## 目标变量定义

### g_target (struct TargetData *)

- temp : float
- hum : float

## 映射关系

1. g_sensor_data->temperature -> g_target->temp [直接赋值]
2. g_sensor_data->humidity -> g_target->hum [直接赋值]
```

**操作：**
- 映射文档**可编辑**：可直接在文本框中修改
- 💾 保存映射文档：保存为 .md 文件，方便复用
- 📥 导入映射到GUI：将映射文档中的关系导入到 GUI 映射列表

### 6.4 阶段3：生成代码

从映射文档生成 C 代码的两种方式：

| 方式 | 说明 | 需要 LLM |
|------|------|----------|
| 🤖 AI 生成代码 | 将映射文档+头文件发送给 LLM 生成代码 | 是 |
| 🐍 本地生成代码 | Python 解析映射文档规则生成代码 | 否 |

**操作：**
1. 确保映射文档已填写
2. 点击"🤖 AI 生成代码"或"🐍 本地生成代码"
3. 查看生成结果
4. 可复制/保存/导入到代码标签页

### 6.5 完整工作流示例

**场景：从需求文档生成代码**

1. 准备需求文档 `需求.txt`：
   ```
   传感器数据转换需求：
   将 g_sensor_data 的 temperature 赋值给 g_target 的 temp，
   humidity 赋值给 hum，需要做单位转换。
   ```

2. 在"映射文档工作流"标签页：
   - 点击"📄 加载文档"加载 `需求.txt`
   - 选择关联头文件
   - 点击"🤖 AI: 自然语言→映射文档"
   - 等待 AI 生成映射文档，检查并编辑
   - 点击"🤖 AI 生成代码"或"🐍 本地生成代码"
   - 查看并保存生成的 C 代码

**场景：从 GUI 映射导出并生成代码**

1. 在 GUI 中配置好映射关系
2. 在"映射文档工作流"标签页：
   - 点击"📤 从GUI映射导出映射文档"
   - 检查生成的映射文档
   - 点击"🤖 AI 生成代码"
   - 保存结果

---

## 7. 从文档导入映射关系

菜单 → 🤖 AI 助手 → 📥 从文档导入映射关系

此功能直接从文档中解析映射关系并导入到 GUI 映射列表，不经过映射文档中间步骤。

**支持的文档格式：**
- 箭头表示法：`source -> target`
- 中文关键词：`source 映射到 target`
- Markdown 表格
- 等号/冒号表示法：`source = target`

详细规则请参考《映射文档编写规则.md》

---

## 8. 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+O | 打开源头文件 |
| Ctrl+T | 打开目标头文件 |

---

## 9. 常见问题

### Q1: LLM 连接失败怎么办？
- 检查 API URL 是否正确（注意末尾不要加 `/chat/completions`）
- 检查 API Key 是否有效
- 如果使用 Ollama，确保 Ollama 服务已启动（`ollama serve`）
- 如果使用 LM Studio，确保已加载模型并启动了本地服务器

### Q2: AI 生成的映射文档不准确？
- 映射文档是可编辑的，你可以直接修改
- 尝试在需求文档中提供更清晰的描述
- 确保关联的头文件包含完整的结构体定义

### Q3: 本地生成和 AI 生成有什么区别？
- **本地生成**：不需要 LLM，Python 解析映射文档中的规则生成代码，速度快但灵活性有限
- **AI 生成**：需要 LLM 服务，能理解更复杂的语义，生成质量更高

### Q4: 映射文档可以复用吗？
- 可以！点击"💾 保存映射文档"保存为 .md 文件
- 下次使用时点击"📂 加载映射文档"直接加载

### Q5: 不想用 AI，能完全本地工作吗？
- 可以。使用"📤 从GUI映射导出"+"🐍 本地生成代码"即可完全本地工作
- 或者手动编写映射文档，然后使用"🐍 本地生成代码"

### Q6: Win7 能用吗？
- 核心功能完全兼容 Win7 + Python 3.6+
- 读取 .docx 需要安装 `python-docx==0.8.11`
- LLM 功能使用 urllib 标准库，无额外依赖

### Q7: 如何编写规范映射文档？
- 请参考《映射文档编写规则.md》获取详细的格式说明和示例
