# 映射文档编写规则

本文档说明如何编写映射关系文档，以便 Extern 变量映射工具能够自动解析并导入映射关系。

---

## 一、文档格式概述

工具支持以下文档格式：
- **Markdown 文件**（`.md`）— 推荐格式
- **纯文本文件**（`.txt`）
- **CSV 文件**（`.csv`）
- **Word 文档**（`.docx`）— 需要安装 `python-docx`

---

## 二、文档结构

一个完整的映射文档通常包含两个部分：

```
## 变量定义
（定义用户变量）

## 映射关系
（定义源成员到目标变量的映射）
```

### 2.1 变量定义区

变量定义区以 `## 变量定义` 或 `## 用户变量` 开头，支持以下格式：

**格式一：空格分隔**
```
float temperature
int count
uint8_t status
```

**格式二：冒号分隔**
```
temperature: float 温度值
count: int 计数器
status: uint8_t 状态标志
```

**格式三：括号标注**
```
temperature (float) 温度值
count (int) 计数器
```

**格式四：Markdown 表格**
```
| 变量名 | 类型 | 描述 |
|--------|------|------|
| temperature | float | 温度值 |
| count | int | 计数器 |
| status | uint8_t | 状态标志 |
```

### 2.2 映射关系区

映射关系区以 `## 映射关系` 或 `## 映射规则` 开头，支持以下格式：

---

## 三、映射关系格式

### 3.1 箭头格式（推荐）

使用 `->` 分隔源成员和目标变量：

```
源成员 -> 目标变量
```

**示例：**
```
temperature -> my_temp
voltage -> my_voltage
sensor.status -> dev_status
```

### 3.2 等号格式

```
源成员 = 目标变量
```

**示例：**
```
temperature = my_temp
voltage = my_voltage
```

### 3.3 冒号格式

```
源成员 : 目标变量
```

### 3.4 中文关键词格式

```
源成员 映射到 目标变量
源成员 对应 目标变量
源成员 赋值给 目标变量
```

**示例：**
```
temperature 映射到 my_temp
voltage 对应 my_voltage
sensor.status 赋值给 dev_status
```

### 3.5 Markdown 表格格式

```
| 源成员 | 目标变量 | 操作类型 | 转换规则 | 条件 |
|--------|----------|----------|----------|------|
| temperature | my_temp | 直接赋值 | = | |
| voltage | my_voltage | 直接赋值 | = | |
| sensor.status | dev_status | 条件赋值 | = | status > 0 |
```

---

## 四、操作类型

在映射关系中可以用方括号 `[]` 指定操作类型：

```
源成员 -> 目标变量 [操作类型]
```

支持的操作类型：

| 操作类型 | 说明 |
|----------|------|
| 直接赋值 | 默认，直接将源值赋给目标 |
| 条件赋值 | 满足条件时才赋值 |
| 逻辑判断 | 仅做条件判断 |
| 自定义表达式 | 自定义转换逻辑 |

**示例：**
```
temperature -> my_temp [直接赋值]
sensor.status -> dev_status [条件赋值]
error_code -> err_flag [逻辑判断]
raw_data -> processed_data [自定义表达式]
```

---

## 五、转换规则

转换规则用于描述值的转换方式：

```
源成员 -> 目标变量 [操作类型] {转换规则}
```

| 转换规则 | 说明 | 示例 |
|----------|------|------|
| `=` | 直接赋值（默认） | `temperature -> my_temp` |
| `(类型)` | 强制类型转换 | `count -> my_float [(float)]` |
| `* 系数` | 缩放（乘法） | `raw_temp -> celsius [* 0.1]` |
| `+ 偏移` | 偏移（加法） | `value -> adjusted [+ 273]` |

---

## 六、条件表达式

条件表达式用花括号 `{}` 指定：

```
源成员 -> 目标变量 [条件赋值] {条件表达式}
```

### 6.1 简单条件

```
temperature -> my_temp [条件赋值] {temperature > 50}
```

### 6.2 组合条件

使用 `AND`（且）或 `OR`（或）连接多个条件：

```
temperature -> my_temp [条件赋值] {temperature > 50 AND status == 1}
voltage -> alarm [条件赋值] {voltage > 250 OR voltage < 180}
```

也支持中文关键词：

```
temperature -> my_temp [条件赋值] {temperature > 50 且 status == 1}
voltage -> alarm [条件赋值] {voltage > 250 或 voltage < 180}
```

### 6.3 支持的操作符

| 操作符 | 说明 |
|--------|------|
| `==` | 等于 |
| `!=` | 不等于 |
| `>` | 大于 |
| `<` | 小于 |
| `>=` | 大于等于 |
| `<=` | 小于等于 |

---

## 七、完整示例

### 示例一：简单映射文档

```markdown
# 传感器数据映射

## 变量定义
my_temp: float 目标温度值
my_voltage: float 目标电压值
dev_status: uint8_t 设备状态

## 映射关系
temperature -> my_temp
voltage -> my_voltage
sensor.status -> dev_status
```

### 示例二：带条件和转换的映射文档

```markdown
# 传感器数据转换规则

## 变量定义
| 变量名 | 类型 | 描述 |
|--------|------|------|
| celsius | float | 摄氏温度 |
| voltage_mv | float | 电压(毫伏) |
| alarm_flag | uint8_t | 告警标志 |

## 映射关系
raw_temp -> celsius [直接赋值] {raw_temp * 0.1}
raw_voltage -> voltage_mv [直接赋值] {raw_voltage * 3.3 / 4096}
error_code -> alarm_flag [条件赋值] {error_code != 0}
```

### 示例三：纯表格格式

```markdown
# 数据映射表

| 源成员 | 目标变量 | 操作类型 | 转换规则 | 条件 |
|--------|----------|----------|----------|------|
| temperature | celsius | 直接赋值 | = | |
| humidity | rh_percent | 直接赋值 | = | |
| pressure | hpa | 直接赋值 | = | |
| status | alarm | 条件赋值 | = | status > 3 |
| error_code | err_flag | 逻辑判断 | = | error_code != 0 |
```

### 示例四：中文关键词格式

```markdown
# 变量对应关系

## 映射关系
temperature 映射到 celsius
voltage 映射到 voltage_mv
sensor.status 对应 dev_status
error_code 赋值给 err_flag [条件赋值] {error_code > 0}
```

---

## 八、注意事项

1. **编码格式**：文档请使用 UTF-8 编码保存
2. **注释行**：以 `#` 或 `//` 开头的行会被忽略
3. **空行**：空行会被自动跳过
4. **表格分隔行**：Markdown 表格中的 `|---|---|` 分隔行会被自动跳过
5. **变量名**：变量名支持字母、数字、下划线、点号（用于结构体成员访问）
6. **结构体成员**：使用 `.` 访问嵌套成员，如 `sensor.temperature`
7. **去重**：导入时自动跳过已存在的映射关系和变量
8. **合并模式**：导入时可选择保留现有映射（追加）或替换全部

---

## 九、导入方式

1. 通过菜单：`🤖 AI 助手` → `📥 从文档导入映射关系...`
2. 选择编写好的映射文档文件
3. 在预览对话框中确认解析结果
4. 选择合并模式或替换模式
5. 点击"确认导入"
